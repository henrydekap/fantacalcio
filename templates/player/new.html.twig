{% extends 'base.html.twig' %}

{% block title %}New Player{% endblock %}

{% block body %}
    <h1>Create new Player</h1>

    {{ include('player/_form.html.twig') }}

    <a class="btn btn-secondary mt-1" href="{{ path('home') }}">back to home</a>
{% endblock %}
